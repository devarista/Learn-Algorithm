import { validParentheses } from '../dist/'

describe('Valid Parentheses', () => {
    test('validParentheses', () => {
        // expect(validParentheses('}')).toBeFalsy()
        // expect(validParentheses('()')).toBeTruthy()
    })
})
